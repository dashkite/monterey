# Testing Monterey

This document explains the testing approach for the Monterey library.

## General Approach

The testing approach utilizes `@dashkite/amen` to provide an assertion framework. The tests validate the core routing, querying, and navigation logic.

## Running Tests

To invoke the test suite, use the `genie` task manager.

```bash
npx genie test
```
